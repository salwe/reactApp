import React from "react";

export const NavBar = (props) => {
    return(
        <nav className="navbar navbar-dark bg-dark fixed-top" id="mainNav">
	        <div className="container">
	            <a className="navbar-brand" href="#page-top">ReactApp</a>
	        </div>
	    </nav>
    );
};